<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.team3.user.cstList.dao.mapper">
  	<select id="cstListCount" parameterType="String" resultType="int">
  		<![CDATA[select count(*) from counsel where id=#{id}]]>
  	</select>
  	
  	<select id="cstList" parameterType="java.util.HashMap" resultType="com.team3.user.cstList.dto.CstListDto">
  		<![CDATA[select * from(select rownum rnum, a.* from(select * from  counsel order by group_number desc, sequence_number asc)a) where rnum >= #{startNum} and rnum <=#{endNum} and id=#{id}]]>
  	</select>
  </mapper>